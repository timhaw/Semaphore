#!/usr/bin/env groovy
package com.intercress
import groovy.json.JsonSlurper

class JSONParser {
    def slurper = new JsonSlurper()
    def String parsedJson = slurper().parseText(text) {
        return parsedJson
    }
}
